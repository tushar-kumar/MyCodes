
    // //to count even and odd elements

    // int even = 0, odd = 0;

    // struct node *temp;
    // temp = head;
    // while (temp != NULL)
    // {
    //     if ((temp->data) % 2 == 0)
    //         even++;
    //     else
    //         odd++;
    //     temp = temp->next;
    // }
    // printf("\n Number of even Numbers are; - %d", even);
    // printf("\nNumber of Odd numbers are :- %d", odd);